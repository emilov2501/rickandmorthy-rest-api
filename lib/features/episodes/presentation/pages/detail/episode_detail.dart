import 'package:flutter/material.dart';

class EpisodeDetail extends StatelessWidget {
  const EpisodeDetail({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Детальная')),
    );
  }
}
